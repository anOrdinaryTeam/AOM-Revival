function postCreate()
    loadHud('PsychEngine', 'Fred, mamalo.');

function create() {

    defaultCamZoom = 0.5;

    useCamMov = true;
    camMoveAmt = 50;

    boyfriend.setPosition(870, 100);
    dad.setPosition(100, 100);

}